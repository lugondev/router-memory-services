.venv/
__pycache__/
*.pyc
*.egg-info/
*.db
*.sqlite3
.pytest_cache/
.ruff_cache/

# .env.example tells you to `cp .env.example .env` and put a live key in it, so
# .env must never be committable. The example stays tracked; the real one cannot.
.env
.env.*
!.env.example
